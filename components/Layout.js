import Link from 'next/link'
import {useMemo,useState} from 'react'
import products from '../data/products.json'
import categories from '../data/categories.json'
export default function Layout({children}){const[q,setQ]=useState('');const found=useMemo(()=>q?products.filter(p=>p.name.toLowerCase().includes(q.toLowerCase())).slice(0,6):[],[q]);return <><header><div className="strip">Оригинальная техника · Гарантия · Проверка при получении · Доставка по России</div><div className="head"><Link href="/" className="brand"><img src="/logo-id.jpeg"/><b>iDanny<small>premium tech store</small></b></Link><Link className="catalog" href="/catalog">Каталог</Link><div className="search"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Поиск iPhone, MacBook, Ray‑Ban..."/>{found.length>0&&<div className="drop">{found.map(p=><Link key={p.slug} href={`/product/${p.slug}`}>{p.name}<b>{p.price}</b></Link>)}</div>}</div><nav className="actions"><Link href="/compare">Сравнение</Link><Link href="/favorites">Избранное</Link><Link href="/cart">Корзина</Link><Link href="/admin">Admin</Link></nav></div><nav className="nav"><Link href="/">Главная</Link>{categories.map(c=><Link key={c.slug} href={`/category/${c.slug}`}>{c.name}</Link>)}</nav></header><main>{children}</main><footer><b>iDanny</b><Link href="/delivery">Доставка</Link><Link href="/warranty">Гарантия</Link><Link href="/contacts">Контакты</Link></footer></>}
